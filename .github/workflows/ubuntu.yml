name: ubuntu

on:
  workflow_dispatch:

jobs:
  ubuntu-novnc:
    runs-on: ubuntu-latest
    timeout-minutes: 3600

    steps:
      - name: Configure apt and install dependencies
        run: |
          # 使用非交互模式，避免 tzdata 等包弹出交互
          export DEBIAN_FRONTEND=noninteractive

          # 更新 apt 索引
          sudo apt-get update -y

          # 安装 Xfce4 轻量桌面环境（最小化安装，避免拉一堆无用软件）
          sudo apt-get install -y --no-install-recommends \
            xfce4 xfce4-terminal xfce4-taskmanager

          # 安装 TigerVNC Server（VNC 后端）
          sudo apt-get install -y --no-install-recommends tigervnc-standalone-server tigervnc-common

          # 安装 noVNC 与 websockify（Web 客户端）
          sudo apt-get install -y --no-install-recommends novnc websockify

          # 一些常用的桌面辅助工具
          sudo apt-get install -y --no-install-recommends \
            dbus-x11 x11-utils firefox espeak ipcalc jq

      - name: Create desktop user
        env:
          RDP_USERNAME: ${{ secrets.RDP_USERNAME }}
          RDP_PASSWORD: ${{ secrets.RDP_PASSWORD }}
        run: |
          if [ -z "$RDP_USERNAME" ]; then
            echo "::error::RDP_USERNAME is not configured in GitHub Secrets."
            exit 1
          fi
          if [ -z "$RDP_PASSWORD" ]; then
            echo "::error::RDP_PASSWORD is not configured in GitHub Secrets."
            exit 1
          fi
          echo "RDP_USERNAME=$RDP_USERNAME" >> "$GITHUB_ENV"

          # 创建用户（如已存在则跳过），加入 sudo 组并免密
          if ! id -u "$RDP_USERNAME" >/dev/null 2>&1; then
            sudo useradd -m -s /bin/bash -G sudo "$RDP_USERNAME"
          fi
          echo "$RDP_USERNAME:$RDP_PASSWORD" | sudo chpasswd
          echo "$RDP_USERNAME ALL=(ALL) NOPASSWD:ALL" | sudo tee "/etc/sudoers.d/$RDP_USERNAME"

      - name: Configure TigerVNC server
        env:
          RDP_USERNAME: ${{ secrets.RDP_USERNAME }}
          RDP_PASSWORD: ${{ secrets.RDP_PASSWORD }}
        run: |
          # VNC 显示号 :1 => 监听 5901
          VNC_DISPLAY=":1"
          VNC_PORT=5901

          # 以目标用户身份写入 VNC 密码文件
          sudo -u "$RDP_USERNAME" -H bash -c '
            mkdir -p "$HOME/.vnc"
            printf "%s\n" "'"$RDP_PASSWORD"'" | vncpasswd -f > "$HOME/.vnc/passwd"
            chmod 600 "$HOME/.vnc/passwd"
          '

          # 写入 xstartup 脚本（先写到临时文件，再安装到用户家目录）
          printf '%s\n' \
            '#!/bin/bash' \
            'unset SESSION_MANAGER' \
            'unset DBUS_SESSION_BUS_ADDRESS' \
            'export XDG_SESSION_TYPE=x11' \
            'exec startxfce4' > /tmp/xstartup
          sudo install -d -o "$RDP_USERNAME" -g "$RDP_USERNAME" "/home/$RDP_USERNAME/.vnc"
          sudo install -m 755 -o "$RDP_USERNAME" -g "$RDP_USERNAME" \
            /tmp/xstartup "/home/$RDP_USERNAME/.vnc/xstartup"

          # 写入 VNC 配置（分辨率、色深、仅监听 localhost）
          printf '%s\n' \
            'geometry=1280x720' \
            'depth=24' \
            'localhost=yes' > /tmp/vnc-config
          sudo install -m 644 -o "$RDP_USERNAME" -g "$RDP_USERNAME" \
            /tmp/vnc-config "/home/$RDP_USERNAME/.vnc/config"

          # 让 VNC 监听 localhost（noVNC/websockify 会转发）
          echo "VNC_DISPLAY=$VNC_DISPLAY" >> "$GITHUB_ENV"
          echo "VNC_PORT=$VNC_PORT" >> "$GITHUB_ENV"

      - name: Start VNC server
        env:
          RDP_USERNAME: ${{ secrets.RDP_USERNAME }}
        run: |
          # 以目标用户身份启动 VNC，绑定到 localhost
          sudo -u "$RDP_USERNAME" -H bash -lc "vncserver $VNC_DISPLAY -localhost yes -geometry 1280x720 -depth 24"

          # 等待 VNC 端口就绪
          for i in $(seq 1 20); do
            if ss -ltn | grep -q ":$VNC_PORT "; then
              echo "VNC server is listening on port $VNC_PORT"
              break
            fi
            sleep 1
          done

          if ! ss -ltn | grep -q ":$VNC_PORT "; then
            echo "::error::VNC server failed to start on $VNC_DISPLAY"
            sudo -u "$RDP_USERNAME" -H bash -lc "cat \$HOME/.vnc/*:1.log" || true
            exit 1
          fi

      - name: Start noVNC / websockify
        run: |
          # noVNC 默认 Web 端口 6080
          NOVNC_PORT=6080
          echo "NOVNC_PORT=$NOVNC_PORT" >> "$GITHUB_ENV"

          # 找到 noVNC 的安装路径
          NOVNC_PATH=$(find /usr/share/novnc -name "vnc.html" 2>/dev/null | head -n1 | xargs dirname 2>/dev/null || true)
          if [ -z "$NOVNC_PATH" ] || [ ! -d "$NOVNC_PATH" ]; then
            # 某些发行版路径不同
            NOVNC_PATH="/usr/share/novnc"
          fi
          echo "noVNC web root: $NOVNC_PATH"

          # 启动 websockify，将 6080 转发到本地 VNC 端口
          nohup websockify --web="$NOVNC_PATH" "$NOVNC_PORT" "localhost:$VNC_PORT" \
            > /tmp/websockify.log 2>&1 &

          # 等待 websockify 就绪
          for i in $(seq 1 20); do
            if ss -ltn | grep -q ":$NOVNC_PORT "; then
              echo "noVNC/websockify is listening on port $NOVNC_PORT"
              break
            fi
            sleep 1
          done

          if ! ss -ltn | grep -q ":$NOVNC_PORT "; then
            echo "::error::websockify failed to start"
            cat /tmp/websockify.log || true
            exit 1
          fi

      - name: Install Tailscale
        run: |
          curl -fsSL https://tailscale.com/install.sh | sh

          # 确保 tailscaled 服务启动
          sudo systemctl enable --now tailscaled

      - name: Authenticate Tailscale
        env:
          TAILSCALE_AUTH_KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        run: |
          if [ -z "$TAILSCALE_AUTH_KEY" ]; then
            echo "::error::TAILSCALE_AUTH_KEY is not configured in GitHub Secrets."
            exit 1
          fi

          echo "TAILSCALE_AUTH_KEY is configured. Authenticating Tailscale..."

          HOSTNAME="gh-runner-${GITHUB_RUN_ID}"
          sudo tailscale up \
            --authkey="$TAILSCALE_AUTH_KEY" \
            --hostname="$HOSTNAME" \
            --unattended

          # 等待 Tailscale 分配 IP
          TS_IP=""
          for i in $(seq 1 10); do
            TS_IP=$(sudo tailscale ip -4 || true)
            if [ -n "$TS_IP" ]; then
              break
            fi
            sleep 5
          done

          if [ -z "$TS_IP" ]; then
            echo "::error::Tailscale authentication failed. IP not assigned."
            sudo tailscale status || true
            exit 1
          fi

          echo "TAILSCALE_IP=$TS_IP" >> "$GITHUB_ENV"
          echo "Tailscale authenticated successfully! IP: $TS_IP"

      - name: Set Machine Name and IP via Tailscale API
        env:
          TAILSCALE_API_TOKEN: ${{ secrets.TAILSCALE_API_TOKEN }}
        run: |
          if [ -z "$TAILSCALE_API_TOKEN" ]; then
            echo "::error::TAILSCALE_API_TOKEN is not configured in GitHub Secrets."
            exit 1
          fi

          echo "TAILSCALE_API_TOKEN is configured. Getting Device ID..."

          # 从本地 tailscale status 获取自身 Device ID
          DEVICE_ID=$(sudo tailscale status --json | jq -r '.Self.ID')
          if [ -z "$DEVICE_ID" ] || [ "$DEVICE_ID" = "null" ]; then
            echo "::error::Could not determine Device ID."
            exit 1
          fi
          echo "Device ID: $DEVICE_ID"
          echo "TAILSCALE_DEVICE_ID=$DEVICE_ID" >> "$GITHUB_ENV"

          API_BASE="https://api.tailscale.com/api/v2"

          # 1. 设置机器名为 "ubuntu"
          echo "Setting machine name to 'ubuntu'..."
          curl -s -X POST "$API_BASE/device/$DEVICE_ID/name" \
            -H "Authorization: Bearer $TAILSCALE_API_TOKEN" \
            -H "Content-Type: application/json" \
            -d '{"name":"ubuntu"}' || echo "::warning::Failed to set machine name"

          # 2. 设置机器 IP 为 100.100.1.2
          echo "Setting machine IP to '100.100.1.2'..."
          curl -s -X POST "$API_BASE/device/$DEVICE_ID/ip" \
            -H "Authorization: Bearer $TAILSCALE_API_TOKEN" \
            -H "Content-Type: application/json" \
            -d '{"ip":"100.100.1.2"}' || echo "::warning::Failed to set machine IP"

          echo "Tailscale API configuration completed!"

      - name: Enable Subnet Router and Exit Node
        run: |
          echo "Enabling Subnet Router and Exit Node..."

          # 获取本机 IPv4 地址，计算子网
          ADVERTISE_ROUTES=""
          for ip in $(ip -4 -o addr show scope global | awk '{print $4}'); do
            ADDR=$(echo "$ip" | cut -d/ -f1)
            PREFIX=$(echo "$ip" | cut -d/ -f2)
            NETWORK=$(ipcalc -n "$ip" | awk -F= '{print $2}')
            SUBNET="$NETWORK/$PREFIX"
            echo "Interface: $ip -> $SUBNET"
            if [ -n "$ADVERTISE_ROUTES" ]; then
              ADVERTISE_ROUTES="$ADVERTISE_ROUTES,$SUBNET"
            else
              ADVERTISE_ROUTES="$SUBNET"
            fi
          done
          echo "Advertise Routes: $ADVERTISE_ROUTES"

          sudo tailscale set \
            --advertise-routes="$ADVERTISE_ROUTES" \
            --advertise-exit-node \
            --accept-routes \
            --accept-dns=false

          echo "=== Tailscale Status ==="
          sudo tailscale status
          echo "Subnet Router and Exit Node enabled."

      - name: Verify noVNC Accessibility
        run: |
          echo "Tailscale IP: $TAILSCALE_IP"
          echo "noVNC port: $NOVNC_PORT"

          # 测试本地 noVNC 端口连通性
          if ! ss -ltn | grep -q ":$NOVNC_PORT "; then
            echo "::error::noVNC port $NOVNC_PORT is not listening"
            exit 1
          fi
          echo "noVNC/websockify is accessible on port $NOVNC_PORT"

      - name: Maintain Connection
        env:
          RDP_USERNAME: ${{ secrets.RDP_USERNAME }}
          RDP_PASSWORD: ${{ secrets.RDP_PASSWORD }}
        run: |
          echo ""
          echo "=== noVNC Cloud Desktop ACCESS ==="
          echo "Web URL:  http://$TAILSCALE_IP:$NOVNC_PORT/vnc.html"
          echo "VNC IP:   $TAILSCALE_IP"
          echo "VNC Port: $VNC_PORT  (for VNC client)"
          echo "Username: $RDP_USERNAME"
          echo "Password: $RDP_PASSWORD"
          echo "================================="
          echo ""

          # 监控 VNC 进程是否存活
          while true; do
            echo "[$(date)] Desktop Active - Use Ctrl+C in workflow to terminate"
            # 检查关键进程
            if ! pgrep -x vncserver >/dev/null 2>&1 && ! pgrep -f "Xvnc" >/dev/null 2>&1; then
              echo "::warning::VNC server process not found, attempting restart..."
              sudo -u "$RDP_USERNAME" -H bash -lc "vncserver $VNC_DISPLAY -localhost yes -geometry 1280x720 -depth 24" || true
            fi
            if ! pgrep -x websockify >/dev/null 2>&1; then
              echo "::warning::websockify process not found, attempting restart..."
              NOVNC_PATH=$(find /usr/share/novnc -name "vnc.html" 2>/dev/null | head -n1 | xargs dirname 2>/dev/null || echo "/usr/share/novnc")
              nohup websockify --web="$NOVNC_PATH" "$NOVNC_PORT" "localhost:$VNC_PORT" > /tmp/websockify.log 2>&1 &
            fi
            sleep 300
          done
